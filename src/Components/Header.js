import React from 'react'
// import PropTypes from 'prop-types'

const styles = {
    div: {
        padding: "0",
        // marginBottom: "6px",
        width: "100%",
        height: "65px",
        backgroundColor: "#20232a",
        color: "white",
        // boxShadow: '4px 1px 6px 7px rgba(0, 0, 0, 0.2)',
    }
}

function Header() {
    return (
        <div style={styles.div}></div>
    )
}

export default Header